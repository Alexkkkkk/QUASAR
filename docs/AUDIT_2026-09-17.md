# QUASAR — полный аудит и синхронизация кода (2026-09-17)

Репозиторий: https://github.com/Alexkkkkk/QUASAR
Проверенный коммит: `277c1d9` (ветка `main`, 150 коммитов, дерево чистое, `0/0` относительно `origin/main`)
Инструменты: Node v22.23.2, npm ci, `tact 1.6.13`, `tsc 5.9.3`, `@ton/sandbox 0.44.0`, `node --test`

Это отчёт инженерного аудита на основе фактического выполнения команд. Он не заменяет
независимый формальный аудит и не является обещанием безопасности.

## 1. Фактически выполненные проверки

| Проверка | Команда | Результат |
|---|---|---|
| Сборка контрактов | `npm run build` | OK, 2 проекта (`quasar`, `quasar_defi`) |
| Воспроизводимость сборки | `sha256sum build/*.code.boc` × 2 | Хеши идентичны → сборка детерминирована |
| Совпадение кода кошелька | sha256 `quasar_QuasarWallet` vs `quasar_defi_QuasarWallet` | `2cab70d9…e570` — одинаковые |
| Типы Tact | `npm run lint` (`tact --check`) | OK |
| Инварианты безопасности | `npm run security:check` | OK, 6 инвариантов |
| Тесты | `node --import tsx --test tests/*.ts` | 20/20 pass (10 contracts + 10 security) |
| Зависимости | `npm audit` | 0 уязвимостей |
| Устаревшие зависимости | `npm outdated` | `@types/node` 26.5.1 (latest 22.20.3), `typescript` 5.9.3 (latest 7.0.2) |
| Секреты в репозитории/истории | grep + `git log --all --diff-filter=A` | Не найдены |
| Проверка типов TS | `npx tsc --noEmit` | См. F-11 (в CI не запускается) |

## 2. Архитектура

- `contracts/quasar.tact` (1286 строк) — `QuasarMaster` (Jetton, fee-transfer, buyback, стейкинг, рефералы, вестинг, лотерея, AI-оракул) + `QuasarWallet`.
- `contracts/quasar_defi.tact` (763 строки) — `QuasarDeFi` (CPMM AMM, LP, farm) + дубликат `QuasarWallet`.
- `scripts/` — `deploy_all.ts`, `deploy_defi.ts`, `deploy.js`, `security_check.ts`, `testnet_smoke.ts`.
- `tools`/CI: `.github/workflows/ci.yml` (lint → npm test, Node 24), `docs/SECURITY_AUDIT.md` (внутренний разбор, не сторонний аудит).

## 3. Находки

| ID | Важность | Область | Суть |
|---|---|---|---|
| F-01 | 🔴 Высокая | Метаданные | TEP-64 контент указывает на `https://quasar-ton.netlify.app/metadata.json`; живой URL отдаёт HTTP 404 → метаданные и картинка токена не резолвятся |
| F-02 | 🟠 Средняя | Web/UI | `website/tonconnect-manifest.json` отсутствует в репозитории, хотя на него ссылаются `config.js:7` и `tonconnect.js:45` |
| F-03 | 🔴 Высокая | UI ↔ контракты | 3 из 8 потоков UI (AddLiquidity, SwapToTON, Stake) физически не могут пройти: контракты требуют предварительный QSR-депозит (`pendingQsrDeposits`), а UI его не делает (`tonconnect.js:150-207`) |
| F-04 | 🟠 Средняя | Anti-whale | `maxTxBps` проверяется только в `Mint` (`quasar.tact:393-394`), а минтинг после деплоя запрещён; `maxWalletBps` записан, но нигде не проверяется; `cooldownSeconds` читается только геттером |
| F-05 | 🟠 Средняя | Стейкинг | Обход блокировки: при дозачислении стейка сохраняется старый `lockEnd` (`quasar.tact:576-579`), а `Unstake` проверяет только `now() >= lockEnd` (`:602`) → топ-ап + немедленный вывод всего баланса |
| F-06 | 🟠 Средняя | Пауза | Emergency pause блокирует выходы пользователей (`Stake`/`Unstake`/`ClaimRewards`/`ClaimVested`: `:565,:596,:624,:713`), но `FeeTransfer`, `TriggerLottery`, `DefiPayout`, `TokenNotification` паузу не проверяют — асимметрия |
| F-07 | 🔴 Высокая | AI-оракул | Заявленные «сдержки» не работают: `AIVetoVote` всегда revert (`:964`); `OwnerOverride` откатывает только `SetFee`/`ToggleTrading` (`:995-996`); cooldown применяется лишь при `aiFullAutonomy` (`:293`); сигналы AI можно словить без записи в `aiActionLog` (`:898-957`) |
| F-08 | 🟠 Средняя | Доступ | Один EOA-владелец без мультисига/таймлока: `SetTreasury`, `SetDefiAddress`, `Resume`, `SweepTON`, `SetDefiPayoutCap` (`:1039-1097`) |
| F-09 | 🟡 Низкая | Buyback | `buybackPool` ведётся в QSR, а порог задан как `ton("10")` (`:234`) — сравнение разных единиц; AMM-свопа нет, `tonSpent` жёстко 0 (`:542-557`) |
| F-10 | 🟡 Низкая | Хранилище | Неограниченно растущие map без очистки: `priceHistory` (ключ `now()`, коллизии), `anomalyLog`, `aiActionLog`, `lotteryWinners` (`:152-203`) |
| F-11 | 🟡 Низкая | CI | Нет `tsc --noEmit`, нет `npm audit`; `husky` объявлен, но каталога хуков нет; `npm run lint` дублирует полную компиляцию; CI на Node 24 при `@types/node ^26` |
| F-12 | 🟡 Низкая | Кошелёк | Кулдаун 5 c только на `TokenTransfer` (`:1223`); фиксированные `ton("0.05")` на протокольный сбор (`:1239`) при минимальном fee в 1 наноQSR |
| F-13 | 🟡 Низкая | DeFi | `ClaimFarmRewards` требует `farmEnabled` (`quasar_defi.tact:409`) → выключение фермы блокирует выплату уже начисленного; `pendingQsrDeposits` не истекают (`:163-166`); `SetFeeBps` разрешает до 1% (`:463`) при заявленных 0.30% |
| F-14 | 🟡 Низкая | Deploy | Проверки сети продублированы в модуле `deploy_defi.ts:12-19`; вместо ожидания подтверждения — `setTimeout` 15/5 c (`deploy_all.ts:96,109,119`) |
| F-15 | 🟡 Низкая | Гигиена | `quasar_defi.tact` в CRLF, `quasar.tact` в LF; нет `.gitattributes`/`.editorconfig` |
| F-16 | 🟡 Низкая | UI | Статистика пула — заглушки (`--`), APY фермы захардкожен `~150%`, `#qsr-balance` не обновляется, TODO `tonconnect.js:98` |

### Расхождения документации и кода (исправлено в ветке)

| Место | Было | Стало |
|---|---|---|
| `README.md` (Fee Distribution) | «50% burned / 50% burned immediately / 50% remaining» — математически несогласованно | Burn 50% от fee, затем сплит оставшихся 50% (15/15/10/5% от остатка, трежери — остаток) |
| `README.md` (Feature Matrix) | «Max tx 1% on-chain» | Указано, что лимит применяется только к минтингу и фактически неактивен |
| `README.md` (AI Safeguards) | Вето/«Owner Override 24h» как рабочие механизмы | Указано фактическое поведение: `AIVetoVote` всегда revert, override только для 2 типов действий |
| `README.md` (Architecture) | «5% fees» | «5% of remainder» (то есть 2.5% от комиссии) |

## 4. Остаётся сделать (не входит в синхронизацию документации)

1. Заменить `jettonContent` URL на существующий и опубликовать `metadata.json` + `assets/logo.png` в корне сайта (F-01).
2. Дописать в UI QSR-депозит перед `AddLiquidity`/`SwapToTON`/`Stake` (F-03).
3. Ввести мультисиг/таймлок и перевести `OwnerOverride` на все типы AI-действий (F-07, F-08).
4. Добавить adversarial-bounce тесты, fuzz/свойства для AMM и тест обхода лок-периода (F-05).
5. Заменить `randomInt()` лотереи на проверяемый источник случайности; вынести buyback в реальный своп (F-09).
