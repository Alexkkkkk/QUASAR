import { toNano, Address } from '@ton/core' ; import { NetworkProvider } from '@ton/blueprint' ; export async function run(provider: NetworkProvider) { const owner = provider.sender().address!! ; const qsrMaster = Address.parse('EQ...') // TODO: replace with actual QUASAR Master address after deployment const { QuasarDeFi } = await import('../build/QuasarDeFi/tact_QuasarDeFi') ; const defi = provider.open(await QuasarDeFi.fromInit(owner, qsrMaster)) ; await defi.send(provider.sender(), { value: toNano('0.5') }, { $$type: 'Deploy', queryId: 0n }) ; await provider.waitForDeploy(defi.address) ; console.log('QuasarDeFi deployed at:', defi.address) }